


# ESD-relearning
# decoding unlearning - with same hyperparameter

# Implement text encoder relearning

# ul_exp_names = ['ul1.prg1e-4d5e-4.lr1e-4.n8.G.sceleb5g0.person.s50_c.l16.kv_sceleb5g0N50-V_pr0.50_lr5e-5.ti5e-4_f0.5_b4g4.s10000']

# ul_exp_names = ['ul1.prg1e-4d8e+3.lr1e-4.n8.G.mcarey.person.s50_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.mcarey.person.s50.r1_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.mcarey.person.s50.r2_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.octavia.person.s50_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.octavia.person.s50.r1_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.octavia.person.s50.r2_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.oprah.person.s50_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.oprah.person.s50.r1_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.oprah.person.s50.r2_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.morganf.person.s50_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.morganf.person.s50.r1_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.morganf.person.s50.r2_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.drake.person.s50_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.drake.person.s50.r1_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.drake.person.s50.r2_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.idris.person.s50_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.idris.person.s50.r1_sd14', 'ul1.prg1e-4d8e+3.lr1e-4.n8.G.idris.person.s50.r2_sd14']

print_training_scripts = True
print_generation_scripts = True
gen_options = ['relearn','preserve']
# gen_options = ['relearn']
# gen_options = ['preserve']
# gen_options = ['preserve_pretrained']
seeds = [0]
unlearn_steps = [0,100,200,300]
unlearn_steps = [300]
# unlearn_steps = [200,300]
# unlearn_steps = [300]
# unlearn_steps = [400,500]


ul_exp_names = ['esd-x.nG3.00_GP.gH.pH-u0.50.pe00-PS0.00_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.10_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.20_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.30_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.40_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.50_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.60_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.70_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.80_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS0.90_U.mrobbie_sd1.4_r0', 'esd-x.nG3.00_GP.gH.pH-u0.50.pe00-cPS1.00_U.mrobbie_sd1.4_r0']
ul_exp_names = ul_exp_names[0:4]
# ul_exp_names = ul_exp_names[4:8]
# ul_exp_names = ul_exp_names[8:12]
# ul_exp_names = ['esd-x.nG3.00_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.10_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.20_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.30_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.40_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.50_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.60_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.70_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.80_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.90_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS1.00_U.mrobbie_sd1.4', 'esd-x.nG3.00_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.10_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.20_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.30_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.40_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.50_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.60_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.70_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.80_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.90_U.ganesha_sd1.4', 'esd-x.nG3.00.pe00-cPS1.00_U.ganesha_sd1.4']
# ul_exp_names = ['esd-x.nG3.00_GP.gH.pH.pe00-PS0.00_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.10_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.20_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.30_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.40_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.50_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.60_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.70_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.80_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.90_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS1.00_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-PS0.00_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.10_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.20_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.30_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.40_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.50_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.60_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.70_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.80_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.90_U.ganesha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS1.00_U.ganesha_sd1.4']

# ul_exp_names = [e for e in ul_exp_names if 'ganesha' in e]



# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS{ps:.2f}_U.mrobbie_sd1.4" for ps in [0.10,0.20,0.30,0.40,0.50]]
# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS{ps:.2f}_U.mrobbie_sd1.4" for ps in [0.60,0.70,0.80,0.90]]
# ul_exp_names = [f"esd-x.nG2.00.pe00-PS{ps:.2f}_U.mrobbie_sd1.4" for ps in [0.10,0.20,0.30,0.40,0.50]]
# ul_exp_names = [f"esd-x.nG2.00.pe00-PS{ps:.2f}_U.mrobbie_sd1.4" for ps in [0.60,0.70,0.80,0.90]]


# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS{ps:.2f}_U.ganesha_sd1.4" for ps in [0.10,0.20,0.30,0.40,0.50]]
# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS{ps:.2f}_U.ganesha_sd1.4" for ps in [0.60,0.70,0.80,0.90]]
# ul_exp_names = [f"esd-x.nG2.00.pe00-PS{ps:.2f}_U.ganesha_sd1.4" for ps in [0.10,0.20,0.30,0.40,0.50]]
# ul_exp_names = [f"esd-x.nG2.00.pe00-PS{ps:.2f}_U.ganesha_sd1.4" for ps in [0.60,0.70,0.80,0.90]]
# ul_exp_names = [f'esd-x.nG3.00_GP.gH.pH.pe00-cPC{ps:.2f}_U.mrobbie_sd1.4' for ps in [0.10,0.20,0.30,0.40,0.50]]
# ul_exp_names = [f'esd-x.nG3.00_GP.gH.pH.pe00-cPC{ps:.2f}_U.mrobbie_sd1.4' for ps in [0.60,0.70,0.80,0.90,1.00]]
# ul_exp_names = [f'esd-x.nG3.00_GP.gH.pH.pe00-cPS{ps:.2f}_U.mrobbie_sd1.4' for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]
# ul_exp_names = [f'esd-x.nG3.00_GP.gH.pH.pe00-cPS{ps:.2f}_U.ganesha_sd1.4' for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]


# ul_exp_names = [f"esd-x.nG3.00.pe00-cPS{ps:.2f}_U.mrobbie_sd1.4" for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]
# ul_exp_names = [f"esd-x.nG3.00.pe00-cPS{ps:.2f}_U.ganesha_sd1.4" for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]

# ul_exp_names = [f'esd-x.nG3.00_GP.gG.pH.pe00-cPS{ps:.2f}_U.mrobbie_sd1.4' for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]
# ul_exp_names = [f'esd-x.nG3.00_GP.gG.pH.pe00-cPS{ps:.2f}_U.ganesha_sd1.4' for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]
# ul_exp_names = [f'esd-x.nG3.00_GP.gB.pH.pe00-cPS{ps:.2f}_U.mrobbie_sd1.4' for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]
# ul_exp_names = [f'esd-x.nG3.00_GP.gB.pH.pe00-cPS{ps:.2f}_U.ganesha_sd1.4' for ps in [0.10,0.20,0.30,0.40,0.50,0.60,0.70,0.80,0.90,1.00]]

# # ul_exp_names = ['esd-x.nG3.00_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.10_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.20_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.30_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.40_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.50_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.60_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.70_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.80_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS0.90_U.mrobbie_sd1.4', 'esd-x.nG3.00.pe00-cPS1.00_U.mrobbie_sd1.4', 'esd-x.nG3.00_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.10_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.20_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.30_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.40_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.50_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.60_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.70_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.80_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS0.90_U.gensha_sd1.4', 'esd-x.nG3.00.pe00-cPS1.00_U.gensha_sd1.4']
# ul_exp_names = ['esd-x.nG3.00_GP.gH.pH.pe00-PS0.00_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.10_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.20_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.30_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.40_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.50_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.60_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.70_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.80_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.90_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS1.00_U.mrobbie_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-PS0.00_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.10_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.20_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.30_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.40_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.50_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.60_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.70_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.80_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS0.90_U.gensha_sd1.4', 'esd-x.nG3.00_GP.gH.pH.pe00-cPS1.00_U.gensha_sd1.4']

# ul_exp_names = [f"esd-x.nG2.00_U.mrobbie_sd1.4"]
# ul_exp_names = [f"esd-x.nG2.00.pe00-PS1.00_U.mrobbie_sd1.4"]
# ul_exp_names = [f"esd-x.nG2.00.pe00-PS1.00_U.ganesha_sd1.4"]
# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS1.00_U.mrobbie_sd1.4"]
# ul_exp_names = ['esd-x.nG2.00_GP.gG.pH.pe00-PS1.00_U.mrobbie_sd1.4']
 
 
# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS0.00_U.mrobbie_sd1.4"]
# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS0.00_U.ganesha_sd1.4"]

# ul_exp_names = [f"esd-x.nG2.00_GP.gG.pH.pe00-PS0.00_U.mrobbie_sd1.4"]
# ul_exp_names = [f"esd-x.nG2.00_GP.gG.pH.pe00-PS0.00_U.ganesha_sd1.4"]

# ul_exp_names = [f"esd-x.nG2.00_GP.gL.pH.pe00-PS0.00_U.mrobbie_sd1.4"]
# ul_exp_names = [f"esd-x.nG2.00_GP.gL.pH.pe00-PS0.00_U.ganesha_sd1.4"]



# ul_exp_names = [f"esd-x.nG3.00_GP.gH.pH.pe00-PS1.00_U.mrobbie_sd1.4"]
# ul_exp_names = [f"esd-x.nG3.00.pe00-PS1.00_U.mrobbie_sd1.4"]
# ul_exp_names = ["esd-x.nG3.00.pe00-PS1.00_U.mrobbie_sd1.4","esd-x.nG3.00_GP.gH.pH.pe00-PS1.00_U.mrobbie_sd1.4" ]
 
# ul_exp_names = [f"esd-x.mrobbie_nG2.00_GP.gH.pH.pe00-PS1.00_sd1.4_r{seed}"]

# ul_exp_names = [f"esd-x.nG2.00_U.ganesha_sd1.4"]

# ul_exp_names = [f"esd-x.nG2.00.pe00-PS1.00_U.ganesha_sd1.4"]
# ul_exp_names = [f"esd-x.nG2.00_GP.gH.pH.pe00-PS1.00_U.ganesha_sd1.4"]

#
manual_prompt = ''

# ul_exp_names = [ul_exp_name for ul_exp_name in ul_exp_names if 'esd-x' in ul_exp_name]

learn_concept = "" # 'moodeng' # 'crybaby' # 'avp' # 'chiquita' # 'reese' # 'gout' # 'jooli' # 'honer' # 'sceleb5g0'
pretrained = 'sd1.4'


#seed = 0

# seeds = [0,1,2] # along

# hacked 
# seeds = [999]*len(ul_exp_names)
# seeds = [0,1,2]
# seeds = [3,4]
# seeds = [0,1,2,3,4]

use_te = True
batch_size = 1
gradient_accumulation_step = 4
# re
# lr_lora_grid = ["1e-4", "5e-5", "1e-5"]
lr_lora_grid = ["1e-4"]
# lr_lora_grid = [ "1e-5"]
lr_ti_grid = ["5e-4"]   # only used if use_ti
# lr_ti_grid   = ["5e-3","5e-2"]   # only used if use_ti
lr_te_grid = ["1e-5"] 
lora_ranks = [4] # [1,2,4,8,16,32,64]
# # Create all combinations of lr_lora, lr_ti, and seed
combos = list(itertools.product(lr_lora_grid,
                                lr_ti_grid,
                                lr_te_grid if use_te else [None],
                                lora_ranks))

use_pr = True

use_te = True
apply_use_ti = True

lr_scheduler = 'linear' # 'linear' # 'cosine' # 'cosine_with_restarts'
apply_negative_prompt = True

use_manual_params = True
reV = True
is_relearn = True 

manual_params = {
    'data_setting': 'align',
    # 'data_setting': 'dummy',
    
    # 'data_setting': 'small',s
    # 'lora_rank' :  16
}


data_setting = 'dummy' 
# learn_concepts = ['cat','dog','horse','car','motorcycle','bicycle']
# final_exp_names = []
# # for ul_exp_name in ul_exp_names:

# for ul_exp_name,learn_concept in zip(ul_exp_names,learn_concepts):

final_exp_names = []
# for ul_exp_name in ul_exp_names:
other_concepts = ['cat','dog','horse','cow','car','bus','motorcycle','bicycle','mountain','river','forest','desert','ganesha']
for ul_exp_name, seed in itertools.product(ul_exp_names, seeds):
    ul_exp_name += f'_r{seed}'
    T_con = None
    if '_T' in ul_exp_name:
        T_con = f"T{ul_exp_name.split('_T')[-1]}"
        
    PS,nG = extract_ps_ng(ul_exp_name)

    if not learn_concept:
        is_found = False
        for c in seen_concepts:
            if c in data_info and c in ul_exp_name:
                concept = c
                is_found = True
                break
        if not is_found:
            for c in other_concepts:
                if c in ul_exp_name:
                    concept = c
                    is_found = True
                    break
    else:
        concept = learn_concept

    if 'esd-x' in ul_exp_name:
        train_method = 'esd-x'
    elif 'esd-u' in ul_exp_name:
        train_method = 'esd-u'
    elif 'esd-all' in ul_exp_name:
        train_method = 'esd-all'

    pretrained_unet_name = ul_exp_name 
    # print(f"pretrained_unet_name: {pretrained_unet_name}")
    ul_exp_name_ = pretrained_unet_name

    
    ul_exp_name = ul_exp_name_
    
    base_exp_name = '_'.join(ul_exp_name.split('_')[1:])
    exp_name = base_exp_name
    
    
    print('base_exp_name:', base_exp_name)

        
    # for seed in seeds:
    for  unlearn_step in  unlearn_steps:
    
    # for seed, unlearn_step in itertools.product(seeds, unlearn_steps):
    # for seed, unlearn_step in itertools.product(seeds, unlearn_steps):

        # print(f"Base Experiment Name: {base_exp_name}")
        
        #fix edit here : they are using this to reconstruct the base_exp as well
        lr_lora, lr_ti = extract_lrs(base_exp_name)
        lora_rank = extract_learning_lora_rank(base_exp_name)
        
        # todo: better use 're'
        for re_lr_lora, re_lr_ti, re_lr_te, re_lora_rank in combos:
            
            # print(f'lora_rank: {lora_rank}, lr: {lr_lora}, ti_lr: {lr_ti}')
            # pretrained_path = f"data_root/logs/{ul_exp_name}/LoRA_fusion_model"
            pretrained_path = 'CompVis/stable-diffusion-v1-4'
            
            
            if unlearn_step is None:
                unet_weight_path = f"data_root/logs/esd/pg/{pretrained_unet_name}.safetensors"
            else: 
                unet_weight_path = f"data_root/logs/esd/pg/{pretrained_unet_name}/step{unlearn_step}.safetensors"
                
            # print(f"Concept: {concept}"

            use_ti = 'ti' in exp_name or '-V' in exp_name or apply_use_ti
            # use_pr = 'pr' in exp_name

            if data_setting == 'facefew':
                dataset_name = f'{concept}5F0r{seed}'
                
            elif data_setting == 'align':
                dataset_name = f'{concept}A5V0'
            elif data_setting == 'fewshot':
                
                if 'sceleb' in concept:
                    dataset_name = f'{concept}U3'
                elif concept == 'avp':
                    dataset_name = 'avpS3'
                else:
                    dataset_name = f'{concept}U3'
            elif data_setting == 'small':
                
                if 'sceleb' in concept:
                    dataset_name = f'{concept}N10'
                else:
                    dataset_name = f'{concept}10'
            else:
                dataset_name = 'dummy'
                # if 'sceleb' in concept:
                #     dataset_name = f'{concept}N50'
                # elif concept == 'avp':
                #     dataset_name = 'avp20'
                # else:
                #     dataset_name = f'{concept}50'
                    
            if reV:
                if concept in concept2initializer:
                    initializer_token = concept2initializer[concept]
                else: initializer_token = 'object'
            else: 
                initializer_token = ''
                
            if use_ti:
                if 'sceleb' in concept:
                    prompt = 'A photo of a v1,A photo of a v2,A photo of a v3,A photo of a v4,A photo of a v5'
                    placeholder_token = 'v1,v2,v3,v4,v5'
                else:
                    prompt = 'a photo of v1' 
                    placeholder_token = 'v1'
            else:
                prompt = concept2prompt[concept]

            name_tag = ''
            if is_relearn: name_tag += 'uul'
            name_tag = f'{name_tag} {dataset_name}'
            name_tag += f' l{lora_rank}'
            if use_ti: 
                # name_tag += f' ti.{lr_ti}'
                name_tag += f' ti'

            data_root = dataset_name2data_root[dataset_name]
            
            if use_ti:
                dataset_name_for_exp = dataset_name + "-V"
                # if use_ni:
                #     dataset_name_for_exp += ".ni"
            else: dataset_name_for_exp = dataset_name
            
            
            # prior preservation folder
            prior_folder = 'original_realistic_vision'
            if pretrained == 'sd1.5':
                prior_folder = 'original_pretrained_sd1.5'
            if pretrained == 'sd1.4':
                prior_folder = 'original_pretrained_sd1.4'     
            if pretrained == 'rv':
                prior_folder = 'original_realistic_vision'
            elif pretrained == 'ch':
                prior_folder = 'original_chilloutmix'

            
            # renaming to check
            # re_exp_name = f'c.l{lora_rank}.kv_{dataset_name_for_exp}'
            # if use_pr:
            #     re_exp_name += f'_pr0.50'
            # re_exp_name += '_lr'
            # if lora_rank >0: re_exp_name += f"{str(lr_lora)}"
            # if use_ti:
            #     re_exp_name += f'.ti{str(lr_ti)}'
            # re_exp_name += '_f0.5_b1g4'
            
            # print(re_exp_name)
            # assert re_exp_name in base_exp_name, f"Expected {re_exp_name} in {base_exp_name}"

            # if manual_lora is not None and manual_data != lora_rank:
            
            if use_manual_params:
                lora_rank = re_lora_rank
                eff_data_setting = manual_params['data_setting']

                lr_lora, lr_ti = re_lr_lora, re_lr_ti
                
                if eff_data_setting == 'facefew':
                    dataset_name = f'{concept}5F0r{seed}'
                    
                elif eff_data_setting == 'align':
                    dataset_name = f'{concept}A5V0'
                
                elif eff_data_setting == 'fewshot':
                    
                    if 'sceleb' in concept:
                        dataset_name = f'{concept}U3'
                    elif concept == 'avp':
                        dataset_name = 'avpS3'
                    else:
                        dataset_name = f'{concept}U3'
                elif eff_data_setting == 'small':
                    
                    if 'sceleb' in concept:
                        dataset_name = f'{concept}N10'
                    else:
                        dataset_name = f'{concept}10'
                else:
                    dataset_name = 'dummy'
                    # if 'sceleb' in concept:
                    #     dataset_name = f'{concept}N50'
                    # elif concept == 'avp':
                    #     dataset_name = 'avp20'
                    # else:
                    #     dataset_name = f'{concept}50'            
                        
                data_root = dataset_name2data_root[dataset_name]
            
            
            
            
            if reV:
                if use_te:
                    relearn_exp_name = f"rlct{lora_rank}.reV.{dataset_name}"
                else:
                    relearn_exp_name = f"rlc{lora_rank}.reV.{dataset_name}"
            else:
                if use_te:
                    relearn_exp_name = f"rlct{lora_rank}.{dataset_name}"
                else:
                    relearn_exp_name = f"rlc{lora_rank}.{dataset_name}"
            
            
            if lr_scheduler == 'linear':
                relearn_exp_name += f".ln"
            relearn_exp_name += f".lr{re_lr_lora}.ti{re_lr_ti}"
                
                
            if use_pr:
                relearn_exp_name += f".pr1.00"
                if apply_negative_prompt:
                    relearn_exp_name += ".neg"
            
            relearn_exp_name += f".b{batch_size}g{gradient_accumulation_step}"
            
            # if seed != 0:
            relearn_exp_name += f".r{seed}"

            final_exp_name = f"{relearn_exp_name}_{ul_exp_name}"
            
            if unlearn_step is not None:
                final_exp_name += f".uS{unlearn_step}"
            
            
            script = f"""
            accelerate launch train_dreambooth_lora.py \\
            --pretrained_model_name_or_path="{pretrained_path}"  \\
            --load_unet_weight_path="{unet_weight_path}" \\
            --instance_data_dir="{data_root}" \\
            --output_dir="data_root/logs/{final_exp_name}" \\
            --validation_prompt="{prompt}" --instance_prompt="{prompt}" \\
            --train_batch_size={batch_size} --gradient_accumulation_steps={gradient_accumulation_step} \\
            --lora_rank {lora_rank} --target_lora_modules to_k to_v --target_lora_layers cross \\
            --max_train_steps=1000  --validation_steps=50  --checkpointing_steps=50  --lr_scheduler "{lr_scheduler}"  --seed {seed} \\
            --run_note '{name_tag}' \\"""
                
            script += f"""
            --cfg_scale 6.0 \\"""
        
        
            if apply_negative_prompt:
                script += f"""
            --negative_prompt "longbody, lowres, bad anatomy, bad hands, missing fingers, extra digit, fewer digits, cropped, worst quality, low quality." \\"""
                
                
            if use_pr:
                if pretrained == 'sd1.4':
                    cfg_pr = 7.5
                elif pretrained == 'ch':
                    cfg_pr = 6.0
                script += f"""
            --with_prior_preservation --prior_loss_weight=1.0 --num_class_images 200 \\
            --class_prompt="{concept2Prprompt[concept]}" --class_data_dir="data_root/generated/model/{prior_folder}/{concept2Prprompt[concept] + '_neg' if 'person' in concept2Prprompt[concept] else concept2Prprompt[concept]}/{cfg_pr:.2f}" \\"""

            # --with_prior_preservation --prior_loss_weight=1.0 --num_class_images 200 \\
            # --class_prompt="a photo of a person" --class_data_dir="data_root/generated/model/{prior_folder}/a photo of a person_neg/{cfg_pr:.2f}" \\"""
                
            # Conditional learning rate + TI options
            if use_ti:
                
                if lora_rank <= 0:
                    script += f"""
            --learning_rate_ti {lr_ti} \\
            --placeholder_token="{placeholder_token}" --initializer_token='{initializer_token}'"""
                else:
                    if use_te:
                        script += f"""
            --learning_rate_lora {lr_lora} --learning_rate_ti {lr_ti} \\
            --train_text_encoder --learning_rate_lora_text_encoder {re_lr_te} \\
            --placeholder_token="{placeholder_token}" --initializer_token='{initializer_token}'"""
                    else:
                        script += f"""
            --learning_rate_lora {lr_lora} --learning_rate_ti {lr_ti} \\
            --placeholder_token="{placeholder_token}" --initializer_token='{initializer_token}'"""
            else:
                script += f"""
            --learning_rate {lr_lora}"""

            if print_training_scripts:
                print(f"echo 'count: {len(final_exp_names)}'")
                print(script)
            final_exp_names += [final_exp_name]
        


print(final_exp_names)

exp_names = final_exp_names

for gen_option in gen_options:
        
    apply_only_unlearn = True if gen_option == 'preserve' else False
    apply_use_ti = True
    apply_negative_prompt = False
    

    # manual_prompt = '*coco30k.500'
    # manual_prompt = "a photo of a black person;a photo of a white person;a photo of a asian person;a photo of a black man;a photo of a white man;a photo of a asian man;a photo of a asian woman;a photo of a white man;a photo of a white woman"
    # manual_prompt += ';'
    # manual_prompt = 'a photo of a cat;a photo of a dog;a photo of a horse;a photo of a mountain;a photo of a car;a photo of a motorcycle;a photo of a bicycle'
    # manual_prompt += ';'
    # manual_prompt += 'a photo of a cow;a photo of a bus;a photo of a mountain;a photo of a river;a photo of a forest;a photo of a desert'
    # manual_prompt = 'a photo of a cow;a photo of a bus;a photo of a mountain;a photo of a river;a photo of a forest;a photo of a desert'
    # manual_prompt = 'a photo of a black woman'
    manual_prompt = 'pe_00' if 'preserve' in gen_option else ''
    # manual_prompt = 'a photo of object'
    # manual_prompt = ''
    # manual_prompt = '*seen'
    # manual_prompt = ''
    donot_reinit_validation_generator = False
    if apply_only_unlearn and manual_prompt =='':
        apply_negative_prompt = True
        # manual_prompt = '*unlearned'
        # manual_prompt = '*nonunlearned'
        manual_prompt = '*seen'
        # manual_prompt = 'a photo of Amy Adams'
        
    # manual_prompt = 'a photo of obama'
    # manual_prompt = '*unlearned'
    use_general_concept = False

    # exp_names = [exp_names[0]]
    cfg_scales = [  4.5, 6.0]
    cfg_scales = [7.5]


    count = 0
    
    

    exp_names_for_gen = list(exp_names)
    if gen_option == 'preserve_pretrained':
        exp_names_for_gen = ['sd1.4']
    
    for exp_name in exp_names_for_gen:

        is_original_pretrained = ( exp_name == 'CompVis/stable-diffusion-v1-4' or exp_name == 'sd1.4' )


        is_relearn = 'rl' in exp_name 
        if is_relearn:
            base_exp_name = '_'.join(exp_name.split('_')[2:])
            relearn_exp_name = exp_name
            unlearn_exp_name = '_'.join(exp_name.split('_')[1:])
            exp_name = base_exp_name
        else:
            unlearn_exp_name = ''

        is_unlearn =  apply_only_unlearn or (not is_relearn and  any(k in exp_name for k in ('ul','uce', 'esd', 'stereo')))
        force_unlearn = is_relearn and apply_only_unlearn
        is_relearn = is_relearn and not force_unlearn
        
        
        # print(is_relearn,is_unlearn)
        # steps =range(0, 3000+1, 200)
        # steps =range(0, 1000+1, 100)
        # steps =range(500, 500+1, 100)
        steps =range(0, 1000+1, 100)

        if 's3000' in relearn_exp_name:
            steps = range(0, 3000+1, 200)
            
        
        if is_unlearn or is_original_pretrained:
            steps = [0]

        for step in steps:
            
            pretrained_path = 'CompVis/stable-diffusion-v1-4'
            # if is_relearn:
                # pretrained_path = f"data_root/logs/{unlearn_exp_name}/LoRA_fusion_model"
                # pretrained_path = 'CompVis/stable-diffusion-v1-4'
            is_found = False
            for c in seen_concepts:
                # print(c)
                if c in data_info and c in unlearn_exp_name:
                    concept = c
                    # print("Found concept:", concept)
                    
                    is_found = True
                    break
            for c in other_concepts:
                if c in unlearn_exp_name:
                    concept = c
                    is_found = True
                    break

            # print(c,concept)
            # print(concept)
            if concept in data_info:
                concept_name = data_info[concept]['Full_name']
            else:
                concept_name = concept
            concept_name_ = concept_name.replace(' ','_')
            
            
            # hack
            if manual_prompt == 'pe_00':
                if apply_only_unlearn or is_original_pretrained:
                    preservation_concepts =  torch.load('data_root/data/preservation_concepts/all_pe_v1_r123.pth',weights_only=False)[concept_name.lower()]['test']['Strongly Associated']
                    # manual_prompt = ';'.join([f'a photo of {c}' for c in [concept_name] + preservation_concepts])
                    manual_prompt = ';'.join([f'a photo of {c}' for c in preservation_concepts])
                else:
                    manual_prompt = f'a photo of {concept_name}'

            unlearning_methods = ['ul1', 'mace','esd-x','esd-u','esd-all','uce','stereo',]
            for _unlearn_method in unlearning_methods:
                if _unlearn_method in unlearn_exp_name:
                    unlearn_method = _unlearn_method
                    break
            # print(f'unlearning method: {unlearn_method}')
            
            # T_con = None
            # if f'{unlearn_method}.T' in unlearn_exp_name:
            #     T_con = unlearn_exp_name.split('.')[1]

            if  is_original_pretrained:
                unet_weight_path = ""


            elif unlearn_method == 'esd-x':
                
                pretrained_unet_name,uS = unlearn_exp_name.split('.uS')
                unet_weight_path = f"data_root/logs/esd/pg/{pretrained_unet_name}/step{uS}.safetensors"
                
                
                # pretrained_unet_name = f"{unlearn_method}.{concept2shortname[concept_name] if concept_name in concept2shortname else concept_name}.sd1.4"
                # # pretrained_unet_name = f"esd-{concept_name_}-from-{concept_name_}-esdx"
                # PS,nG = extract_ps_ng(unlearn_exp_name)
                
                # if nG is not None:
                #     pretrained_unet_name += f"_nG{nG:.2f}"
                # if PS is not None:
                #     pretrained_unet_name += f"_PS{PS:.2f}"
                # if T_con is not None:
                #     pretrained_unet_name += f"_{T_con}"
                # if 'BGeneral' in unlearn_exp_name:
                #     pretrained_unet_name += f"_BGeneral"
                    
                # if 'dTavg' in unlearn_exp_name:
                #     pretrained_unet_name += f"_dTavg"
                    
                # if "s500" in unlearn_exp_name:
                #     pretrained_unet_name += f"_step500"
                    
                # unet_weight_path = f"data_root/logs/esd/pg/{pretrained_unet_name}.safetensors"
            elif unlearn_method == 'esd-u':
                if T_con is not None:
                    pretrained_unet_name = f"esd-{concept_name_}-from-{concept_name_}-esdu_{T_con}"
                else:
                    pretrained_unet_name = f"esd-{concept_name_}-from-{concept_name_}-esdu"
                unet_weight_path = f"data_root/logs/esd/pg/{pretrained_unet_name}.safetensors"
            elif unlearn_method == 'esd-all':
                pretrained_unet_name = f"esd-{concept_name_}-from-{concept_name_}-esdall"
                unet_weight_path = f"data_root/logs/esd/pg/{pretrained_unet_name}.safetensors"         
            elif unlearn_method == 'stereo':
                # print(unlearn_exp_name)
                if 'ACeleb' in unlearn_exp_name:
                    full_name = data_info[concept]['Full_name']
                    if full_name == 'Barrack Obama':
                        full_name = 'Barack Obama'

                    unet_weight_name = f"{full_name}.ACeleb"
                    if extract_nG_pG(unlearn_exp_name)[0] is not None:
                        nG, pG = extract_nG_pG(unlearn_exp_name)
                        unet_weight_name += f".nG{nG:.2f}pG{pG:.2f}"
                        
                        
                    unet_weight_path = f"data_root/logs/stereo/{unet_weight_name}/final_reo_unet.pt"
                else:
                    unet_weight_path = f"data_root/logs/stereo/{data_info[concept]['Full_name']}/final_reo_unet.pt"
            elif unlearn_method == 'uce':
                unet_weight_path = f"data_root/logs/uce/{concept}_uce_sd.safetensors"
                
                
            elif unlearn_method == 'mace':
                unet_weight_path = ""
                pretrained_path = f"data_root/logs/{unlearn_exp_name}/LoRA_fusion_model"
                
                
            # if is_unlearn: 
            #     pretrained_path = f"data_root/logs/{exp_name}/LoRA_fusion_model"

            use_ti = 'ti' in relearn_exp_name or '-V' in exp_name 
            
            if is_relearn and not 'reV' in relearn_exp_name:
                # relearn is not re-initializing the token (by default)
                initializer_token = ''
            elif use_ti:
                if concept in concept2initializer: initializer_token = concept2initializer[concept]
                else:
                    initializer_token = 'object'
            if manual_prompt:
                if manual_prompt == '*seen' or manual_prompt == '*unlearned' or manual_prompt == '*nonunlearned' or manual_prompt[0] == '*':
                    
                    for seen_concept in seen_concepts:
                        if seen_concept in unlearn_exp_name:
                            unlearned_concept = seen_concept
                            break
                            
                    if  manual_prompt == '*unlearned':
                        prompt = f"a photo of {data_info[unlearned_concept]['Full_name']}"
                    elif manual_prompt == '*nonunlearned':
                        non_unlearned_prompts = [ f"a photo of {data_info[seen_concept]['Full_name']}" for seen_concept in seen_concepts if seen_concept!=unlearned_concept]
                        prompt = ';'.join(non_unlearned_prompts)
                    elif manual_prompt == '*seen':
                        prompts = [f"a photo of {data_info[seen_concept]['Full_name']}" for seen_concept in seen_concepts]
                        prompt = ';'.join(prompts)
                        
                    elif manual_prompt == '*catcarmountain':
                        prompt = 'a photo of a cat;a photo of a car;a photo of a mountain'
                        apply_negative_prompt = False
                    elif '*coco30k' in  manual_prompt  :
                        prompt = manual_prompt
                        apply_negative_prompt = False
                        
                        # we do not want to use the same seed for every caption
                        donot_reinit_validation_generator = True
                        
                else:
                    prompt = manual_prompt
            # elif use_general_concept:
            #     prompt = concept2generalprompt[concept]
            
            elif use_ti:
                if 'sceleb' in concept:
                    prompt = 'A photo of a v1,A photo of a v2,A photo of a v3,A photo of a v4,A photo of a v5'
                    placeholder_token = 'v1,v2,v3,v4,v5'
                else:
                    prompt = 'a photo of v1' 
                    placeholder_token = 'v1'
            else:
                prompt = concept2prompt[concept]
                
            ## hacky .. should change this later
            
            
            if is_original_pretrained:
                load_lora_weight_path = ''
                gen_image_path = 'data_root/generated/model/original_pretrained_sd1.4'
                unet_weight_path = ''
                
                
            elif is_relearn:
                exp_name = relearn_exp_name
                load_lora_weight_path =f"data_root/logs/{exp_name}/checkpoint-{step}"
                gen_image_path = 'auto'
            # if is_unlearn or 'erase' in exp_name or exp_name == 'original_pretrained': 
            #     load_lora_weight_path = ''
                # gen_image_path = f"data_root/generated/model/{exp_name}"
            elif is_unlearn:
                # load_lora_weight_path =f"data_root/logs/{exp_name}/checkpoint-{step}"
                load_lora_weight_path = ''
                gen_image_path = 'auto'
                if force_unlearn:
                    
                    if 'uS' in unlearn_exp_name:
                        pretrained_unet_name,uS = unlearn_exp_name.split('.uS')
                        gen_image_path = f"data_root/generated/model/{pretrained_unet_name}/step{uS}"
                        
                    else:
                        gen_image_path = f"data_root/generated/model/{unlearn_exp_name}"



            # if 'l0' in exp_name :
            #     load_lora_weight_path = ''
            
            script = f"""
            accelerate launch train_dreambooth_lora.py \\
                --pretrained_model_name_or_path='{pretrained_path}'  \\
                --load_unet_weight_path="{unet_weight_path}" \\
                --load_lora_weight_path="{load_lora_weight_path}" \\
                --instance_data_dir="data_root/data/real_data/dummy" \\
                --gen_image_path="{gen_image_path}" \\
                --output_dir="data_root/logs/gen" \\
                --validation_prompt="{prompt}" --instance_prompt="{prompt}" \\
                --lora_rank 1 --target_lora_modules to_k to_v --target_lora_layers cross \\
                --run_note 'gen img' --wait_weight \\
                --num_validation_images 50 \\"""
                # --num_validation_images 50 --gen_batch 10 \\"""


            if use_ti and not is_unlearn and  not is_original_pretrained:
                script += f"""
                --load_token_embedding_path="data_root/logs/{exp_name}/checkpoint-{step}" \\
                --placeholder_token="{placeholder_token}" --initializer_token='{initializer_token}' \\"""

            if apply_negative_prompt:
                script += f"""
                --negative_prompt "longbody, lowres, bad anatomy, bad hands, missing fingers, extra digit, fewer digits, cropped, worst quality, low quality." \\"""
            
            if donot_reinit_validation_generator:
                script += f"""
                --donot_reinit_validation_generator \\"""

            script += f"""
                --cfg_scale {','.join(f'{x:.2f}' for x in cfg_scales)} --gen_batch 10"""
                
                
            if print_generation_scripts:
                print(f"echo 'count:{count} - {exp_name} {step} /'")
                print(script) 
            
            count += 1
    if print_generation_scripts: print(f"Total scripts generated: {count}")
        